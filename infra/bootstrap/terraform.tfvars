# 実行前に必ず値を確認・修正すること。

project_id = "real-conversation-agents" # 要確認: このプロジェクトを使ってよいか着手前に確認する
region     = "us-central1"

# グローバルに一意な名前である必要がある。既に使われていないか事前に確認する。
state_bucket_name = "real-conversation-agents-tfstate"

github_repository = "tasugiya/real_conversation_agents"
