# api/ agent/ (Python) のみを対象にする。frontendはESLint/Prettier等に委ねる。
# docs/DEVOPS.md pre-commit節を参照。
# 現時点ではapi/agent配下に.gitkeepしかないため実質no-opだが、コードが入り次第効き始める。

repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.0
    hooks:
      - id: ruff
        args: [--fix]
        files: ^(api|agent)/
      - id: ruff-format
        files: ^(api|agent)/

  - repo: https://github.com/pre-commit/mirrors-mypy
    rev: v1.14.1
    hooks:
      - id: mypy
        files: ^(api|agent)/

  - repo: https://github.com/Yelp/detect-secrets
    rev: v1.5.0
    hooks:
      - id: detect-secrets
        args: ["--baseline", ".secrets.baseline"]

  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-added-large-files
      - id: detect-private-key
